@model IEnumerable<BricksWarehouse.WebModels.ProductTypeEditWebModel>
@{
    ViewBag.Title = "Виды товаров в корзине";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-sm-2">
            <partial name="Partial/_LeftSidebarPartial" />
        </div>
        <div class="col-sm-10">
            <h1>@ViewBag.Title</h1>

            <hr />
            <a asp-controller="ProductTypeEdit" asp-action="Index" class="btn btn-secondary">
                Вернутся к редактированию
            </a>
            <hr />

            <div class="table-responsive-md">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th><label>№</label></th>
                            <th>@Html.DisplayNameFor(m => m.Name)</th>
                            <th>@Html.DisplayNameFor(m => m.FormatNumber)</th>
                            <th>@Html.DisplayNameFor(m => m.Order)</th>
                            <th>@Html.DisplayNameFor(m => m.Units)</th>
                            <th>@Html.DisplayNameFor(m => m.Volume)</th>
                            <th>@Html.DisplayNameFor(m => m.Weight)</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.Any())
                        {
                            var index = 1;
                            @foreach (var productType in Model)
                            {
                                <tr>
                                    <td>@(index++)</td>
                                    <td>@Html.DisplayFor(_ => productType.Name)</td>
                                    <td>@Html.DisplayFor(_ => productType.FormatNumber)</td>
                                    <td>@Html.DisplayFor(_ => productType.Order)</td>
                                    <td>@Html.DisplayFor(_ => productType.Units)</td>
                                    <td>@Html.DisplayFor(_ => productType.Volume)</td>
                                    <td>@Html.DisplayFor(_ => productType.Weight)</td>
                                    <td class="text-center">
                                        <a asp-controller="ProductTypeEdit" asp-action="Edit" asp-route-id="@productType.Id" class="btn btn-success">
                                            <i class="fas fa-check"></i>Восстановить
                                        </a>
                                        <a asp-controller="ProductTypeEdit" asp-action="Delete" asp-route-id="@productType.Id" class="btn btn-danger">
                                            <i class="fas fa-times"></i>Удалить совсем
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <p>Виды товаров в корзине отсутсвуют.</p>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
