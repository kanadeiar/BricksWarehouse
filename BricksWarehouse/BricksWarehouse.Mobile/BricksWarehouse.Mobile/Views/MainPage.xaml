<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BricksWarehouse.Mobile.ViewModels"
             xmlns:v="clr-namespace:BricksWarehouse.Mobile.Views"
             x:Class="BricksWarehouse.Mobile.Views.MainPage"
             Title="{Binding Title}"
             x:DataType="vm:MainPageViewModel"
             BindingContext="{Binding MainPageViewModel, Source={StaticResource Locator}}">

    <StackLayout>
        <Frame BackgroundColor="#f31731" Padding="24" CornerRadius="0">
            <Label Text="{Binding Title}" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
        </Frame>
        <Label Text="Мобильное приложение" FontSize="Title" Padding="10, 0" />
        <Label Text="Обзор общей итоговой информации по складу в обобщенном виде. Управление складом осуществляется этим мобильным приложением." FontSize="Default" Padding="30,10,30,10"/>
        <Label Text="Количество товаров на складе" FontSize="Title" Padding="30,10,30,10"/>
        <Label Text="Количество товаров каждого отдельного вида на складе в обобщенном виде" FontSize="Default" Padding="30,10,30,10"/>

    </StackLayout>

</ContentPage>
